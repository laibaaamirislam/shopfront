<!-- app/views/admin/products/show.html.erb -->
<h1 class="h3 mb-4"><%= @product.name %></h1>
<p><%= @product.description %></p>
<p><%= number_to_currency(@product.price) %> — <%= @product.stock_quantity %> in stock</p>
<%= link_to "Edit", edit_admin_product_path(@product), class: "btn btn-dark" %>